[theme]
primaryColor = "#E63946"  # 鮮やかな赤（ボタン・強調）
backgroundColor = "#EAF4FF"  # 薄いブルー（全体背景）
secondaryBackgroundColor = "#CFE3FF"  # 少し濃いブルー（カード・サイドバー）
textColor = "#2B2B2B"  # 濃いグレー
font = "sans serif"
